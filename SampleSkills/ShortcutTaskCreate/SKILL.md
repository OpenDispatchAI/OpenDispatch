# Shortcut Task Create

Routes task creation through a user-owned shortcut named `OpenDispatch Add Task`.

Capability:
- `task.create`

Executor:
- `shortcuts`

Parameters:
- `title`

Expected shortcut input:
- JSON text payload with the task title.
